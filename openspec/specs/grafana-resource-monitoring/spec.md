# grafana-resource-monitoring Specification

## Purpose
TBD - created by archiving change grafana-resources-dashboard. Update Purpose after archive.
## Requirements
### Requirement: Declarative Grafana Dashboard Provisioning

The system SHALL declare the resource monitoring Grafana dashboard under the custom NixOS module configurations in `src/roles/nixos/files/etc/nixos/modules/grafana.nix` so it is provisioned automatically.

#### Scenario: Dashboard registered in Grafana config

- **WHEN** Grafana configuration is evaluated
- **THEN** the system registers the resource monitoring dashboard JSON definition under the declarative provisioning path

#### Scenario: Dashboard file is provisioned from Nix

- **WHEN** `./scripts/play.sh --limit habiki --tags nixos` applies the Grafana module
- **THEN** `/etc/grafana/dashboards/system-resources.json` is created from the Nix-managed dashboard JSON

### Requirement: Dynamic Host Variable Dropdown

The dashboard SHALL include a template dropdown variable named `$host` that queries Prometheus/Mimir for all unique values of the `host` label to dynamically select machines (e.g., `fyne`, `habiki`, `bullit`).

#### Scenario: Host template variable populated

- **WHEN** the dashboard is loaded by an operator in the Grafana UI
- **THEN** the template variable dropdown named `host` executes a dynamic query for `host` label values against the default Prometheus/Mimir datasource with UID `"mimir"`

#### Scenario: Host list is not hardcoded

- **WHEN** dashboard JSON is reviewed
- **THEN** the `host` template variable uses a query such as `label_values(host)` rather than a static option list of host names

### Requirement: Isolate PromQL Host Telemetry

The dashboard SHALL filter all Prometheus metrics in the CPU, Memory, and Disk panels using `{host="$host"}` to correctly isolate telemetry emitted by the Alloy node-exporter configurations.

#### Scenario: Resource utilization query isolates host

- **WHEN** any resource panel (CPU, Memory, or Disk) is rendered on the dashboard
- **THEN** it executes a PromQL query that includes the `{host="$host"}` label matcher to target the active host selection

#### Scenario: CPU panel filters by host

- **WHEN** the CPU utilization panel is rendered
- **THEN** its PromQL expression includes `{host="$host"}` and uses datasource UID `mimir`

#### Scenario: Memory panel filters by host

- **WHEN** the memory usage panel is rendered
- **THEN** its PromQL expression includes `{host="$host"}` on memory metrics and uses datasource UID `mimir`

#### Scenario: Disk panel filters by host

- **WHEN** the root disk utilization panel is rendered
- **THEN** its PromQL expression includes `{host="$host"}` on filesystem metrics and uses datasource UID `mimir`

### Requirement: Grafana OAuth client ID consistency
Grafana OAuth configuration SHALL use the Authentik Grafana OAuth2 provider client ID generated by OpenTofu and deployed through the generated-secret and agenix workflow.

#### Scenario: Grafana redirects with current Authentik client ID
- **WHEN** a user starts Grafana login through `/grafana/login/generic_oauth`
- **THEN** the `client_id` in the Authentik authorization request matches the current OpenTofu-managed Authentik Grafana OAuth2 provider client ID

#### Scenario: Grafana rejects placeholder OAuth configuration
- **WHEN** the generated Grafana OAuth client ID is empty, missing, or contains a placeholder value
- **THEN** the deployment fails before Grafana service configuration is applied

#### Scenario: Grafana service receives refreshed OAuth environment
- **WHEN** the generated Grafana Authentik client ID or client secret changes
- **THEN** Grafana receives the updated systemd environment file and is restarted through the IaC deployment flow
