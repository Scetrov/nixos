# Home Assistant Configuration Audit

Last reviewed: 2026-05-23

## Repository-managed state

- `src/roles/nixos/files/etc/nixos/modules/home-assistant.nix` defines the Home Assistant container, host-network mode, `/var/lib/homeassistant:/config`, generated `configuration.yaml`, Authentik OIDC settings, onboarding bypass, bootstrap owner service, and SSDP/mDNS firewall allowances.
- Matter support is implemented as the official `ghcr.io/matter-js/python-matter-server:stable` container, bound to `127.0.0.1:5580`, with persistent state in `/var/lib/matter-server` created as `0750 root root` so runtime artifacts remain host-managed and outside Git.
- `src/roles/nixos/files/etc/nixos/home-assistant/hass-oidc-auth-v1.1.0.zip` is the only existing custom component artifact and remains the documented OIDC exception.
- `automations.yaml`, `scripts.yaml`, and `scenes.yaml` are managed as reviewed placeholder files because the live files contain no user automation, script, or scene behavior.
- `dashboards/` is reserved for reviewed declarative dashboard assets. No dashboard has been imported.

## Live runtime inventory

- `/var/lib/homeassistant/configuration.yaml` is generated by Nix activation and should remain repository-defined through `home-assistant.nix`.
- `/var/lib/homeassistant/automations.yaml` is 2 bytes and contains an empty list.
- `/var/lib/homeassistant/scripts.yaml` is empty.
- `/var/lib/homeassistant/scenes.yaml` is empty.
- `/var/lib/homeassistant/blueprints` contains only default Home Assistant blueprints: `motion_light.yaml`, `notify_leaving_zone.yaml`, and `confirmable_notification.yaml`.
- No `.storage/lovelace*` dashboard files are present.
- No helper or template storage files were found for `input_*`, `counter`, `timer`, or `template`.
- Recent `podman-home-assistant.service` warnings showed no entries for the reviewed 24 hour window.

## Import decisions

- Safe to manage: `automations.yaml`, `scripts.yaml`, and `scenes.yaml` as empty placeholders.
- Do not import: `.storage/core.config_entries`, `.storage/core.entity_registry`, onboarding state, auth state, registries, and any future UI-managed dashboard storage without a separate review.
- Preserve runtime-only state by continuing to mount `/var/lib/homeassistant` and installing only reviewed files during activation.

## Automation and script behavior

No automations or scripts were imported in this review.

- GIVEN the managed `automations.yaml` placeholder is installed
- WHEN Home Assistant loads automations
- THEN it loads an empty automation list and performs no automation actions.

- GIVEN the managed `scripts.yaml` placeholder is installed
- WHEN Home Assistant loads scripts
- THEN it loads an empty script mapping and exposes no repository-managed scripts.

No inefficient automations, broad state triggers, repeated polling, redundant service calls, or missing conditions were identified because there are no live automation or script definitions to optimize.

## Integration and protocol review

Configured integration domains found in live state: `androidtv_remote`, `backup`, `bluetooth`, `cast`, `elgato`, `go2rtc`, `hue`, `ipp`, `nanoleaf`, `sun`, and `thread`.

Entity registry summary:

- 336 total entities.
- 292 entities enabled.
- 44 entities disabled by their integration.
- 0 entities hidden.
- Platform counts: `androidtv_remote` 2, `backup` 5, `cast` 2, `elgato` 9, `hue` 303, `ipp` 2, `nanoleaf` 3, `person` 1, `sun` 9.

Cleanup candidates:

- The 44 integration-disabled entities should be reviewed in the Home Assistant UI before removal because the registry data alone does not prove they are orphaned or renamed.
- No unavailable or renamed entity was removed during this change.

Matter and Thread:

- `thread` is configured in live state.
- No `matter` config entry was present.
- The managed Matter Server provides only the local controller websocket at `ws://localhost:5580/ws`; it is not a Thread border router and is not exposed through Caddy, Authentik, or any other public route.
- Keep using official Home Assistant integrations for Thread and Matter. Document which external border router provides Thread connectivity before commissioning Thread-based Matter devices.

Custom integration policy:

- New HACS or custom integrations require a documented exception with rollback notes.
- `hass-oidc-auth` remains the only current custom integration exception.

## Dashboard and helper review

- No live Lovelace dashboard storage files were present, so dashboards remain UI/default-managed.
- No dashboard layout issue can be evaluated from repository state yet.
- No helper, template sensor, or group storage files were found.
- No helpers were removed.
